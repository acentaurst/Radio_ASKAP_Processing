# DStools操作文档

---

## 安装指南（系统Ubuntu24.04）：

### **第一阶段：环境初始化与基础工具准备**

在干净的 Ubuntu 24.04 服务器系统中，首先更新系统源并安装基础编译套件 。

```
# 更新系统包列表
sudo apt update && sudo apt upgrade -y

# 安装基础编译工具及 Git
sudo apt install build-essential cmake g++ git pkg-config -y
```

### **第二阶段：安装 WSClean 编译依赖（避坑关键）**

Ubuntu 24.04 官方的 `apt install wsclean`​ 存在打包漏洞，运行时会报缺失 `libradler.so` 错误，且版本较低，因此必须选择源码编译 。

为了避免 CMake 在编译时去 GitHub 动态下载依赖而引发的服务器网络超时问题，强烈建议直接通过系统源一次性补齐所有的底层射电天文与 C++ 开发库 。

```
sudo apt install python3-dev casacore-dev python3-casacore libblas-dev liblapack-dev libcfitsio-dev libfftw3-dev libgsl-dev libhdf5-dev libopenmpi-dev libboost-filesystem-dev libboost-program-options-dev libboost-date-time-dev libxtensor-dev libxsimd-dev pybind11-dev -y
```

### **第三阶段：WSClean 源码编译与安装**

依赖补齐后，开始拉取 WSClean 源码进行本地编译（这里以满足 DStools 要求的 v3.5 为例） 。

```
# 1. 克隆 WSClean 官方仓库的指定版本
git clone --branch v3.5 https://gitlab.com/aroffringa/wsclean.git
cd wsclean
```

接下来开始构建和安装 ：

```
# 2. 创建并进入构建目录
mkdir build && cd build

# 3. 运行 CMake 配置（此时应能完美识别本地的 PythonLibs、Boost 和 XTensor）
cmake ../

# 4. 调用多核多线程加速编译（nproc 会自动获取服务器 CPU 核心数）
make -j$(nproc)

# 5. 正式安装到系统目录
sudo make install

# 6. 刷新终端路径缓存（让系统立马认出新安装的 wsclean）
hash -r
```

### **第四阶段：Conda 环境配置与 DStools 部署**

确保你的全局环境干净后，切入 Conda 为 DStools 搭建专属的 Python 3.12 空间 。

Bash

```
# 1. 创建并激活专用的 conda 环境
conda create -n dstools python=3.12 -y
conda activate dstools

# 2. 从 GitHub 官方仓库直接拉取并使用 pip 安装最新的 dev 分支
pip install git+https://github.com/askap-vast/dstools.git@dev
# 建议先尝试main分支，dev分支可以自动化mask源但是不准确，适合批量处理，main分支需要的是Python 3.11的环境
```

### **第五阶段：验证**

部署完成后，在 `(dstools_dev)` 环境下依次运行以下三条命令，确保整条流水线完全打通 ：

- **验证 WSclean**：运行 `wsclean -version`​。预期输出为正常打印出 WSClean version 3.5（或 3.4），且无任何 `.so` 动态链接库缺失的报错 。
- **验证 Python 模块导入**​：运行 `python -c "import dstools; print('🎉 DStools 成功导入！路径:', dstools.__file__)"`​。预期输出为成功打印出环境内的 `site-packages/dstools` 路径 。
- **验证命令行快捷脚本**：运行 `s`。预期输出为成功唤出针对 ASKAP 减除模型创建的帮助文档与参数说明 。

---

## ASKAP官方管线说明：

### 第一步：预处理 

- **命令**​：`dstools-askap-preprocess <MS>`
- **官方解释的必要性**：

  1. **修正极化通量刻度**：使其符合 CASA 标准（$I = (XX + YY)/2$）。
  2. **对齐极化轴**：按照 IAU（国际天文学联合会）标准对齐 X 和 Y 接收器。
  3. **校准相位中心**：将波束相位中心参考系设置为正确的坐标。

### 第二步：视场建图与建模

使用 WSClean 对校准后的可见度数据进行成像，并生成视场内源的模型。

- **命令**：`dstools-create-model -B AK_low -v <MS>`
- **官方推荐的可选参数**：

  - 设定阵列配置和频段（帮助底层自动选择成像参数）。

    `-B AK_low`：强制使用ASKAP低频段。
  - `-v`：开启详细日志，方便监控程序

### 第三步：相位自校准 

**命令：**​`dstools-selfcal <ms>`

- **实操要领**：选择标记率最低的核心天线（如 `ak33`​），并在最后一步务必输入 `y` 将解算结果应用到数据中。

### 第四步：模型注入与目标掩模

将生成的模型图像预测回 `.ms`​ 文件的 `MODEL_DATA`​ 列，并在此时**挖除目标源**。

- **命令**：`dstools-insert-model -p 217.3753317 -62.6744863 -r 5(5为默认值，可不写) <MODEL_DIR> <MS>`
- - `-p`：精准源的坐标，定位需要mask的源。
  -  **​`-r 12`​**：mask半径

### 第五步：模型减除

从原始数据中减去刚才注入的“只包含干扰源”的模型，得到纯净的底噪和目标源数据。

- **命令**：`dstools-subtract-model -s <ms>`
- **官方推荐参数**：

  -  **​`-S`​**​  **(或**  **​`--split-data`​**​ **)** ：分离出一个带有 `.subtracted.ms`​ 后缀的新文件，纯净数据会保存在新文件的 `DATA` 列中。避免了列名混乱。

### 第六步：动态谱提取 

从减除后的纯净数据中提取数据。

- **命令**：`dstools-extract-ds -p 217.3753317 -62.6744863 -v <substract.ms> <DS>`
- **官方参数说明**：

  -  **​`-p <RA> <DEC>`​** ：设定提取动态谱的相位中心坐标。
  -  **​`-d`​**​：选择从哪一列提取（`DATA`​, `CORRECTED_DATA`​, 或 `MODEL_DATA`）。
  -  **​`-B`​**​：**禁用基线平均**。官方明确指出，默认情况下（不加 `-B`​），脚本**会**在基线轴上对可见度进行平均以节省内存。当想保留每条基线的独立信息（比如用 `-u`​ 筛选特定长度的基线）时，才需要加上 `-B`。

### 第七步：一键绘图

官方提供了处理脚本，无需手写读取代码，直接生成光变曲线和频谱。

- **命令**：`dstools-plot-ds -d <DS>` 。
- **指令参数**：

  -  **​`-l`​**：绘制通道平均的 1D 光变曲线（Lightcurve）。
  -  **​`-p`​**：绘制时间平均的 1D 频谱图。
  -  **​`-s IQUV`​**：选择要绘制的极化参数。
  -  **​`-Y`​**​：直接生成一张包含光变曲线、频谱和全极化动态二维谱的​**终极汇总图**。
  -  **​`-t`​**​  **/**   **​`-f`​**​：指定时间和频率的平均因子（例如 `-t 5 -f 10` 表示每 5 个时间步长和 10 个频率通道做一次平均）。

---

## 实操步骤

### 第零步：构建纯净数据沙盒 (Deep Copy)

在开始任何破坏性操作前，必须从原始数据库中深拷贝一份工作副本。这能避免文件锁、软链接或残留缓存导致的读写错误 ，**当在大型服务器上运行时，由于CASA对于多核心适配不足，也容易出问题，可采用这方法规避。**

1. **先解压**：

   ```python
   tar -xvf scienceData.VAST_1453-62...ms.tar
   ```
2. **执行深拷贝**（注意 Python 语法中的引号和括号）：

   ```
   python -c "import casacore.tables as pt; t=pt.table('解压后的原始.ms文件夹名'); t.copy('目标名.ms', deep=True, valuecopy=True); t.close()"
   ```

   建议：目标文件明确加上 `.ms`​ 后缀，例如 `SB50381.beam33.ms` 。

### 第一步：预处理

修正极化通量刻度（使其符合 CASA 标准）、对齐极化轴并校准相位中心 。

```
dstools-askap-preprocess <.ms文件>
```

### 第二步：视场建图与深度建模

**放弃官方默认的** **​`dstools-create-model`​**​ ，直接调用底层的 `wsclean` 进行多尺度、宽带、深度自掩模成像。这能极其精准地建立源的模型，是消除背景杂质和源残留伪影的关键 。

1. **新建模型目录**：
2. **运行高精度 WSClean**：

   ```Python
   wsclean -name wsclean_model/wsclean -size 4096 4096 -scale 2.5asec -niter 1000000 -mgain 0.8 -pol iv -weight briggs 0.5 -shift <时h分m秒s> <度d分m秒s> -auto-threshold 5 -auto-mask 6 -multiscale -multiscale-scale-bias 0.7 -multiscale-max-scales 8 -join-channels -channels-out 8 -deconvolution-channels 8 -fit-spectral-pol 3 -temp-dir wsclean_model <你的.ms文件>
   ```

    **坐标格式警告**：这里的 `-shift`​ 是为了在成像时将位置挪到中间，其参数**绝对不能**写成十进制角度（不可用 `-p`​ 替代） 。必须使用赤经（时/分/秒）和赤纬（度/分/秒）格式（例如：`14h29m30.08s -62d40m28.15s`） 。

   `auto-threshold 5`:表示5$\sigma$以下的RMS不消除,迭代次数设置了100000次，但实际上跑不到，会在5$\sigma$处停下。

   `-name`:设定路径

   **若需要跑全图:**

   ```python
   wsclean -name wsclean_model/wsclean -size 8192 8192 -scale 2.5asec -niter 1000000 -mgain 0.8 -pol iv -weight briggs 0.5 -auto-threshold 5 -auto-mask 6 -multiscale -multiscale-scale-bias 0.7 -multiscale-max-scales 8 -join-channels -channels-out 8 -deconvolution-channels 8 -fit-spectral-pol 3 -temp-dir wsclean_model <你的.ms文件>
   ```

### 第三步：相位自校准 (按需跳过)

官方管线命令为 `dstools-selfcal` 。

**实操建议**：如果目标星本身并不总是特别明亮，或者视场不够完美，强制自校准可能会把错误的相位信息引入数据中。因此，这一步**可以跳过** 。

### 第四步：模型注入与目标挖除

将 WSClean 生成的高精度模型预测回 `.ms`​ 文件的 `MODEL_DATA` 列，并在此时将目标源（如比邻星）挖除 。

```
dstools-insert-model -p <赤经_十进制> <赤纬_十进制> -r 12.5(默认五个像素) wsclean_model <.ms文件>
```

WSClean 的 `-predict`​ 指令，能在几分钟内读取的 FITS 模型，在 `.ms`​ 文件里建好 `MODEL_DATA` 列并填入数据，防止过度减除，导致光变曲线交叉，辐射通量密度降低

```python
wsclean -predict -name wsclean_model/wsclean -channels-out 8 -pol iv <.ms>
```

同时一些参数过程需要重设，就将文件收入文件夹，唯独WSclean处理时间过长可保留，但`dstools-insert-model`​ 的逻辑是“把修改好的模型倒入 `MODEL_DATA`​ 里”。因为容器根本不存在，所以没有实际的操作（虽然没报错），导致下一步的 `subtract` 找不到可以减除的模型列，报错，也可用此方法处理。

也可采用官方指令

```bash
dstools-create-model -I 8192 -c 2.5 -N 1000000 -g 0.8 -r 0.5 -t 5 -m 6 -S --multiscale-scale-bias 0.7 --multiscale-max-scales 8 -f 8 --deconvolution-channels 8 -n 3 -j {线程数} -o wsclean_model --name wsclean --temp-dir wsclean_model <.ms文件>
```

### 第五步：模型减除

从原始数据中减去刚才注入的“包含干扰源和背景”的模型，留下纯净的底噪和目标源信号 。

```
dstools-subtract-model -S <.ms文件>
```

**实操建议**：建议使用 `-S`​（或 `--split-data`​），单独分离出一个带有 `.subtracted.ms` 后缀的新文件，避免列名混乱和破坏原数据 。

### 第六步：动态谱提取

从减除后的 `.subtracted.ms` 中提取数据。

```
dstools-extract-ds -p <赤经_十进制> <赤纬_十进制> -v -u 500 -B <你的.subtracted.ms> <输出文件前缀>
```

 **核心参数解析**：

- `-u 500`：切除 500以下的短基线。短基线对大尺度结构敏感，切除它可以物理性地屏蔽掉弥散的背景辐射和宽场干扰 。
- `-B`​：​**禁用基线平均**​。官方默认会在基线轴上做平均以节省内存，如果想用 `-u`​ 保留独立基线信息进行筛选，必须加上 `-B` 。

### 第七步：一键绘图与分析

对生成的 `.ds或.h5` 文件进行可视化 。

Bash

```
dstools-plot-ds -d <提取的.ds或.h5文件> -f 8 -t 6 -I 5 -V 5 -l -p -P -s IV
```

**参数及数据解读指导**：

- **出图指令**​：`-l`​ 绘制光变曲线，`-p`​ 绘制频谱，`-s IV`​ 选择绘制总强度和圆偏振，`-t/-f`​ 代表时间与频率的平均因子 。如果要看**原生分辨率**的1D光变曲线，请将参数改为 `-t 1 -f 1` 。
- **图表色差**：官方底层绘制全极化（Stokes V）动态谱时默认使用的是 `coolwarm` 色标。

---

## 使用技巧

### 一.防断链

**1. 安装** **​`tmux`​**​ **：**

```
apt-get install -y tmux
```

**2. 在里面跑程序：**

|**操作目标**|**终端命令 / 键盘快捷键**|**作用说明**|
| --| --------------------| ------------------------------------|
|**新建任务**|​`tmux new -s <名字>`|创建并进入一个全新的后台环境|
|**查看任务**|​`tmux ls`|列出目前服务器上有多少个后台环境|
|**重连任务**|​`tmux a -t <名字>`|重新进入之前挂起的某个后台环境|
|**挂起退出**|先按`Ctrl+B`​，松开再按`D`|（最重要）把任务留在后台，安全退出|
|**彻底销毁**|在 tmux 内部输入`exit`|任务跑完后，彻底删除当前后台环境|

`tmux new -s carta_server`

### 二.帮助文档

```python
dstools-extract-ds --help
```

### 三.查看单位时间

```python
`python3 -c "import h5py,numpy as np; f=h5py.File('提取的.ds或h5文件','r'); t=f['time'][:]; print('time:', t);print('shape:', t.shape); dt=np.diff(t); print('dt:', dt, 'unique:', np.unique(dt)); f.close()"`
```

‍
